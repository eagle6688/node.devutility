/**
 * All modules.
 */
const forwarder = require("./lib/forwarder");
const scriptUtilities = require("./lib/scriptUtilities");
const styleUtilities = require("./lib/styleUtilities");

module.exports = {
    forwarder: forwarder,
    scriptUtilities: scriptUtilities,
    styleUtilities: styleUtilities
};