/**
 * Move font files to target directory.
 */

const sysPath = require("path");
const ioUtilities = require("utilities-io");

let projectDirectory = process.cwd();

module.exports = function (config) {
    ioUtilities.createDirectory();


};